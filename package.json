{
  "name": "final-project",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "contract": "npm install && truffle compile && truffle migrate --reset",
    "server": "cd server && npm install && npm start",
    "client": "cd client && npm install && npm start"
  },
  "author": "Paz Nahmias & Ronen saviz",
  "license": "ISC",
  "dependencies": {
    "truffle": "^5.5.23",
    "@openzeppelin/contracts": "^4.6.0"
  }
}
